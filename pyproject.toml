[tool.poetry]
name = "kndxhz_tools"
version = "0.1.0"
description = "一个自用的工具集"
authors = "kndxhz <kndxhz@163.com>"

[tool.poetry.dependencies]
python = ">=3.8"

[tool.poetry.dev-dependencies]
pytest = "^6.2"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"